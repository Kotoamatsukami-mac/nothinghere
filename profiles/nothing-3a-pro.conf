# Profile: Nothing Phone 3a Pro — profile zero
# Identity source of truth. No IPs in operator config.
# Tailscale hostname is the stable layer; IP used as fallback only in ctl.

NHERE_HOST="nothing-phone-3a-pro"
NHERE_HOST_IP="100.123.75.12"     # Tailscale IPv4 — fallback if MagicDNS not resolving
NHERE_PORT="8022"
NHERE_USER="u0_a296"
NHERE_KEY="$HOME/.ssh/nhere_ed25519"

# Agent path on phone
# NHERE_AGENT_PATH="~/nhere/agent"   # default — uncomment to override

# Device metadata
NHERE_DEVICE_FAMILY="nothing"
NHERE_ANDROID_FLOOR="14"
